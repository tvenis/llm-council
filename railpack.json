{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "18",
    "python": "3.10"
  },
  "steps": {
    "install": {
      "commands": [
        "if [ -d frontend ]; then cd frontend && npm ci; else echo 'Frontend directory not found'; fi",
        "pip install -r requirements.txt"
      ]
    },
    "build": {
      "inputs": [{"step": "install"}],
      "commands": [
        "if [ -d frontend ]; then cd frontend && npm run build; else echo 'Frontend directory not found'; fi"
      ]
    }
  }
}
