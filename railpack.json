{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "18",
    "python": "3.10"
  },
  "steps": {
    "install": {
      "commands": [
        "ls -la",
        "pwd",
        "[ -d frontend ] && (cd frontend && npm ci) || (echo 'Frontend directory not found, skipping npm install')",
        "pip install -r requirements.txt"
      ]
    },
    "build": {
      "inputs": [{"step": "install"}],
      "commands": [
        "[ -d frontend ] && (cd frontend && npm run build) || (echo 'Frontend directory not found, skipping build')"
      ]
    }
  }
}
