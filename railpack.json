{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "20",
    "python": "3.10"
  },
  "steps": {
    "install": {
      "inputs": [
        "...",
        { "local": true, "include": ["frontend/", "requirements.txt"] }
      ],
      "commands": [
        "cd frontend && npm ci",
        "pip install -r requirements.txt"
      ]
    },
    "build": {
      "inputs": [
        { "step": "install" },
        { "local": true, "include": ["frontend/**"] }
      ],
      "commands": [
        "cd frontend && npm run build"
      ]
    }
  }
}
